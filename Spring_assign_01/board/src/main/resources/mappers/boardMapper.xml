<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.board.mapper.BoardMapper">
  <select id="findAll" resultType="Board">
    SELECT BOARD_ID boardId,
          TITLE title,
          CONTENTS contents,
          FILE_NAME fileName,
          MEMBER_EMAIL memberEmail,
          CREATE_AT createdAt,
          UPDATE_AT updatedAt
    FROM BOARD
  </select>
    <select id="selectBoardById" parameterType="long" resultType="com.kh.board.entity.Board">
        SELECT BOARD_ID boardId,
        TITLE title,
        CONTENTS contents,
        FILE_NAME fileName,
        MEMBER_EMAIL memberEmail,
        CREATE_AT createdAt,
        UPDATE_AT updatedAt
        FROM BOARD
        WHERE BOARD_ID = #{boardId}
    </select>
    <delete id="deleteBoard" parameterType="long">
        DELETE FROM BOARD
        WHERE BOARD_ID = #{boardId}
    </delete>
</mapper>